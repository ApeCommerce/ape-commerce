# Common library

The common library is shared by API, Admin and Store projects.

## Setup

Install dependencies:

```
yarn
```

## Development

Build in watch mode:

```
yarn dev
```

Run linter:

```
yarn lint
```
