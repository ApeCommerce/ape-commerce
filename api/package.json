{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev src/framework/cli",
    "cli": "ts-node src/framework/cli",
    "lint": "eslint src --fix",
    "lint:ci": "eslint src",
    "test": "rm -rf test.log && jest --verbose",
    "test:coverage": "rm -rf test.log coverage && jest --verbose --coverage",
    "test:ci": "jest",
    "build": "rm -rf dist && tsc --project tsconfig.build.json"
  },
  "engines": {
    "node": "^16.20"
  },
  "dependencies": {
    "common": "link:../common/dist"
  },
  "devDependencies": {
    "@types/jest": "^29.5",
    "@types/node": "^18.15",
    "@typescript-eslint/eslint-plugin": "^5.57",
    "@typescript-eslint/parser": "^5.57",
    "eslint": "^8.37",
    "eslint-config-airbnb-base": "^15.0",
    "eslint-config-airbnb-typescript": "^17.0",
    "eslint-plugin-import": "^2.27",
    "eslint-plugin-jest": "^27.2",
    "jest": "^29.5",
    "ts-jest": "^29.0",
    "ts-node": "^10.9",
    "ts-node-dev": "^2.0",
    "typescript": "^5.0"
  }
}
